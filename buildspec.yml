

version: 0.1
phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Run the test and package the code...
      - sbt assembly
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/scala-2.12/akkaHttp-assembly-0.1-SNAPSHOT.jar

